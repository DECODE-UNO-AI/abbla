name: Pipeline update instances by SSH

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_DEFAULT_REGION: us-west-2

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install sshpass
        run: sudo apt-get install sshpass

      - name: Execute command on remote server
        env:
          INSTANCE_1_SSH_HOST: ${{ secrets.INSTANCE_1_SSH_HOST }}
          INSTANCE_1_SSH_USER: ${{ secrets.INSTANCE_1_SSH_USER }}
          INSTANCE_1_SSH_PASSWORD: ${{ secrets.INSTANCE_1_SSH_PASSWORD }}
        run: echo $INSTANCE_1_SSH_PASSWORD | sshpass -p $INSTANCE_1_SSH_PASSWORD ssh -tt -o StrictHostKeyChecking=no -x -t $INSTANCE_1_SSH_USER@$INSTANCE_1_SSH_HOST -S sudo < command-to-instance-1.sh

        # sshpass -p $INSTANCE_1_SSH_PASSWORD ssh -t -o StrictHostKeyChecking=no -x $INSTANCE_1_SSH_USER@$INSTANCE_1_SSH_HOST "sudo -s < command-to-instance-1.sh"
